# PLAN：当前目标实施计划

> 角色：**当前目标方案文档**：基于 `docs\research\`（为什么）与 `docs\references\`（怎么做）撰写的执行计划；每条挂依据来源，随目标变化更新，不存历史目标。
> 分工：`PRD.md` = 要什么；`TODO.md` = 做到哪；本文件 = 怎么做；通用工作流见 `docs\guide\G003-工作流标准细则-从登记到归档五步.md`。

## 当前目标：D30 0.6.1 更名收尾批

> 依据：PRD D30 第 1 轮两裁（2026-09-12）；D29/P0046 机理不变，本批只修残面。四段：

1. **四回归修**（sweep 连坐，净会话核验发现）：
   - `pathutil::data_dir`：neu 存在但仅含 `bin/`（或空）时仍迁 legacy 子项（move 逐子项、collision 保 neu 侧、bin 不动），堵「README 装法先建根挡 rename」缝；既有「两者都有不动旧」语义仅收窄到 neu 有实内容（hooks/state 等）时。
   - `deploy.rs` 项目 shim 退役环：文件名环补旧名 `oma-state.*`（保留新名双查），内容标记双收（`generated by oma init` 与 `generated by hst init`）。
   - `SKILL_MARKER`：换全 hst marker，覆写判定双收新旧 marker（旧项目 SKILL.md 恢复可再生）；frontmatter `name: ohmyagents` 加 description 换 hst。
   - 同型顺手：`hst skill --write` 若旧 `~/.claude/skills/ohmyagents/` 为生成物则随写退役（判 marker，用户手改不碰）。
2. **串面清扫**：README（四处仓 URL 换 hst-rs、Windows 安装块路径修、L6 shim 路径、L90 命令名、L103 网关 env 名）；doctor（hooks.form 两处路径、四处 oma init CTA、三处 project-yolo CTA、对应断言）；verify（两处中文提示）；deploy（warn 根路径、COMMAND_MAP about、SKILL 正文）；main（隐藏别名 help、verbose stderr 标记）；statusline（生成脚本内注释两处）；update/install 两处 UA；源注释顺手批只改现状描述、历史引用保留。`oma-state`/`oma-` 在 is_ours 历史识别与 legacy 检测里故意保留不清。
3. **测试与门禁**：新增 data_dir bin 先建根用例与退役环旧名用例、SKILL 旧 marker 覆写用例；全量测试加 fmt/clippy 加 rumdl 加 .tools 三扫描。
4. **发版加实证**：版本 0.6.1、CHANGELOG、diary；推 main CI 绿后 tag v0.6.1、digest 双源核对、herdr 知会 ohmycloud；随后本机完整老用户剧本迁移实证（ oma self update 拿 stub、stub 找不到 hst 打指路 exit 1、按修好的 README 装 hst 到 `.hst\bin` 加 PATH、备份挪开 dogfood 实根后 hst init 实测 rename 迁移加 heal、doctor 全绿、oma 转调 hst、散件合并回），结论六态落 diary，P0047 归档。
