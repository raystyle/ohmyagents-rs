# 文档全量清单：方案与研究目录的完整索引

> 全量方案、研究、目录清单。规则权威源见 AGENTS.md，命名细则见同目录文档标准。
> 分工：`GOAL.md`（任务目标）+ `docs\PLAN.md`（怎么做）+ `docs\TODO.md`（进度）+ `docs\history\`（方案）+ `docs\diary\`（项目日记）+ `docs\MISTAKES.md`（错误沉淀）。

## 全部方案（`docs\history\`）

> 进行中与否见 `docs\TODO.md`。项目日记见 `docs\diary\`。

- `0001-四路会话工具-CLI控制面与网页观察面.md`：首期切面（CLI 控制、网页观察、项目级、默认四路）
- `0002-项目重新定位-通用多Agents自动配置和任务编排器.md`：上一版定位措辞
- `0003-rmux检测版本哈希与全平台安装.md`：`oma check` 检测 / pin 哈希 / 缺失则安装
- `0004-项目重新定位-通用智能体多路复用任务编排器.md`：现役定位与显示名 Oh My Agents
- `0005-各功能部件POC验证原型.md`：现役目标；按部件做 example POC
- `template.md`：方案模板

## 项目日记（`docs\diary\`）

- `2026-08-29-对照ohmypwsh建立文档骨架.md`：当天工作记录

## 研究文档

> 写作规范见 `docs\research\guide.md`（含六态标记标准）。

- `research\guide.md`：研究文档写作规范；六态标记标准（AGENTS 规则 10）
- `research\四路会话的控制面与观察面.md`：CLI 控四路、网页只镜像；对照三份 demo 与 evo-harness
- `research\drive铁律与三段式粘贴.md`：四铁律与 paste-buffer 三段式
- `research\项目级hook与skill.md`：启动目录加载 hook/skill；init 部署；信任库例外
- `research\agent状态通道.md`：herdr 语义、项目内 JSON、不引入 herdr 运行时
- `research\跨平台与无浏览器.md`：Linux/mac/WSL/Windows；默认不弹窗
- `research\yolo与无阻塞启动-参数或配置.md`：yolo 持久化、无阻塞诊断、spawn 不堵委派；对照 ohmypwsh 0017
- `research\YouMind无头模式与YOLO免审批对照-Claude-Grok-Kimi.md`：学习 YouMind 公开文；无头 ≠ YOLO；对本仓 TUI pane 与将来 `-p` 的分路
- `research\信任阻塞种类-目录hook-skill-MCP.md`：folder / hook / skill / MCP 分门；设置、绕过、检测；Claude 泄漏源 codeaashu/claude-code
- `research\Claude官方文档-阻塞门与编排对齐.md`：以 code.claude.com/docs 为权威；交互 vs `-p` 分表；v2.1.196+ MCP；skill-as-plugin
- `research\检测已装agent-PATH与默认目录与环境变量.md`：`oma agents` 扫描 PATH / 自定义 env / 默认安装位置
- `research\现有研究复核与扩展.md`：六篇对照的公开源复核、纠正与缺口
- `research\rmux状态判断与hook补充.md`：rmux 只能判终端；agent 忙闲靠 hook 分层补充
- `research\win-rmux既有rmux研究吸收.md`：全量吸收 win-rmux（SKILL、14 篇 references、`.rmux_tasks`、命令核验）；对照 CLI `oma`
- `research\rmux-sdk最佳开发实践与验证poc.md`：`rmux-sdk` 0.10 用法、对照 `oma` 原语、POC-0..8 闸门与草稿

## pin

- `catalog\rmux.toml`：rmux tag / 各平台资产 SHA256（`oma check` 信任锚）
- `examples\poc-yolo-doctor.rs`：项目级 yolo 落盘 + 只读 doctor（临时目录）
- `examples\poc-endpoint.rs`：专用 named pipe / unix socket，非 Default
- `examples\poc-session.rs`：CreateOnly 撞名、ReuseOnly、kill-session 保 keeper
- `examples\poc-layout.rs`：2x2 `split_with` + argv
- `examples\poc-drive.rs`：`send_text` 与 `send_key(Enter)` 分发
- `examples\poc-dialogs.rs`：hook 写 blocked + sendkeys 点假对话框
- `src\hook.rs`：`oma hook` 事件映射与 state 落盘
- `src\rmuxpoc.rs`：POC 共用端点 / 闸门 / Job Object WMI 退路

## 细则（`docs\references\`）

- `项目定位-通用智能体多路复用任务编排器.md`：现役定位展开
- `文档标准细则-命名写作规范与rumdl检查.md`
- `常用命令与管理流程-从项目init到会话cleanup.md`
- `文档全量清单-方案与研究目录的完整索引.md`：本文件

## 项目目录索引

```text
ohmyagents/
  AGENTS.md
  CLAUDE.md
  GOAL.md
  README.md
  CHANGELOG.md
  ROADMAP.md
  Cargo.toml
  catalog\
  src\
  examples\
  .rumdl.toml
  docs\
    PLAN.md
    TODO.md
    MISTAKES.md
    history\
    diary\
    research\
    references\
```
