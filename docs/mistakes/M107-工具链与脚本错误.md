# M107-工具链与脚本错误

> 关键词：sed、grep、PowerShell、中文路径、转义、批替换。

| 编号 | 错误现象 | 根因 | 正确处理 | 首次踩 |
| --- | --- | --- | --- | --- |
| M023 | Git Bash sed 批量替换中文加反斜杠路径静默未生效 | sed BRE 转义与多字节路径组合在该环境失配，且无报错；验证 grep 的转义又假阴性 | 跨平台批改中文路径用 PowerShell `String.Replace` 字面替换；验证用 `Select-String` 或 python `io`，不用多层转义的 grep 模式 | 2026-08-31 |
| M024 | ast-grep 模式 `fn name($$$)` 零命中 | Rust 的 fn_item 节点含 body，模式缺 body 通配即不匹配；可见性修饰也要写进模式 | 按名定位用 `pub fn name($$$) $$$`（带 body 通配与 pub）；签名级用 `fn $NAME($$$) -> Result<$RET, String> $$$` | 2026-08-31 |
| M025 | ast-grep `--json` 提取 metavariable 为空 | 0.45.1 的 JSON 键是 `metaVariables.single.<VAR>.text`，不是 `meta` | 解析取 `m['metaVariables']['single']['NAME']['text']`；先看一条原始记录再写提取脚本 | 2026-08-31 |
| M026 | mq 选择器落空或报 Unknown selector `.s` | `.h.1` 是层级值选择器（非标题文本）；无 `.s`；section 模块函数必须 `-A` 聚合才拿得到全文档节点；`map()` 内嵌 section 函数触发 runtime bug | 标题节点用 `.h`/`.h1`；按节抽用 `mq -A 'section::section(., "标题含词")'`（多文件也带 `-A`）；mq 在 `D:\ohmyenv\mq\mq.exe` 不在会话 PATH，需全路径 | 2026-08-31 |
| M028 | rg 输出里目标词全变成了 `ln`，像源码被篡改 | 误写 `rg -rln "hooks"`：`-r ln` 是 `--replace ln` 替换标志，`-l`/`-n` 被吞进替换串参数位，匹配文本全被替换显示 | rg 想只列文件加行号就显式写 `-l -n` 或 `-n --files-with-matches`；看到整词被替换成无意义串先查自己是否带了 `-r` | 2026-08-31 |
| M032 | 全量 `cargo test` 偶发单例失败（单跑全绿，P0006/P0009 两现） | 测试临时目录名只到毫秒粒度，同毫秒并行测试共享目录，一例的 `remove_dir_all` 删掉另一例的夹具 | 临时目录名加进程内原子序号（`AtomicU64` 尾缀，agents/yolo/deploy/orch/tests 五处统一）；偶发二现即修不留观；提交门禁必须含 `cargo test`，只查文档三件套会带病提交（本日实犯一次） | 2026-08-31 |
| M033 | 文档门禁串联 `uv run --script .tools/md-heading-scan.py | tail -1` 后违规未拦提交（同日两犯：S019 断链与括号标题先后穿透提交） | 管道后接 `tail` 时 `$?`/`&&` 判的是 tail 的退出码（恒 0），扫描器 exit 1 被吞；多行输出时 `tail -1` 还把其余违规视觉隐藏 | 门禁命令一律裸跑拿退出码再接后续（`scan && git commit`，不经任何管道）；违规穿透的提交当场补修提交，不 amend。同日三犯变体：分号链 `scan; echo $?; git add && git commit`：gate 与 commit 只被 `;` 邻接不被 `&&` 约束，退出码看了也白看。同日四犯变体：`scan | tail -1 && git commit`：管道尾退出码是 tail 的，gate 失败照样放行；门禁与提交要么 `&&` 直连不接任何管道尾部，要么裸跑看全绿再单独提交 | 2026-08-31 |
| M034 | 用 Python 脚本批改 src/trace.rs，跑完报 ValueError 后源文件变 0 字节 | `io.open(p, "w")` 打开即截断，newline 参数非法在 TextIOWrapper 初始化才抛：截断已发生；叠加 bash heredoc 多层转义把补丁脚本本身写坏 | 脚本改源码一律「写 .tmp 新文件 + 校验 + 原子替换」，禁止对源文件直开写模式；含大量引号/反斜杠/转义序列的载荷（源码补丁、jsonl fixture）一律用 Write 工具落文件，不走 shell heredoc（同日二犯：heredoc 把双反斜杠吃成单反斜杠，Python 源里的 `\a` 缩成 BEL 混进 fixture 路径（本条聚合时 heredoc 又吃一层，终稿用 Edit 工具修））；jsonl fixture 生成后逐行 parse 自检 | 2026-08-31 |
| M035 | python `str.replace` 批量替换 AGENTS.md 时把 `docs\references` 里的 `\r` 当转义解释，行被劈成两半（`docs` 挂行尾、`eferences` 接行首） | `\r` 在字符串字面量里是回车转义；含反斜杠路径的替换文本不经 raw 处理就会被解释 | 批量替换含 Windows 路径的文本用 raw 字面量或 `.tools\md-replace.py`（M023 专用）；改完 grep 该路径确认无断行 | 2026-09-01 |
| M036 | `git add -A` 把隔离构建目录 target-test 全仓卷入提交（8501 个文件，命令链超时才暴露） | 新开的构建/临时目录（CARGO_TARGET_DIR=target-test）没同步进 .gitignore；add -A 不问内容全收 | 新增任何工作目录（构建产物、临时项目）当场补 .gitignore；提交优先精确 add 文件清单，用 -A 前先 `git status` 扫一眼；污染提交用 `git reset HEAD~1` 重做，不 filter 历史 | 2026-09-01 |
| M037 | 对照实验随手建了官方域 + `--no-pin` + operator 的 share 链接并贴进对话（公网中继上一条 URL 即全权、免 PIN、可打字） | 只想着隔离变量（官方前端 vs 本地包），没过一遍安全面叠加：官方域经公网中继，no-pin 关掉第二道门，operator 是键盘权限 | 建公开链接先自问三要素（域、PIN、角色）；测试用链接优先本地前端；用完当场 `web-share --disconnect` 清单归零（本条记档后实测警示已产品化：PUBLIC-RELAY-NO-PIN） | 2026-09-01 |
| M049 | CI Linux/mac `claude_argv_carries_bypass_flag_others_do_not` panic `validated above`；Windows 本机绿；dev-release 自 09-02 起不出滚动资产 | 改 `OMA_*_BIN` 的测试分两把锁（`BIN_ENV_LOCK` 与 `ENV_LOCK`）；`plan_agents` 先 `find` 再 `expect(find)`，并发测试清掉 env 后第二次 None | 探测一次记住 Hit；动 env 的测试都拿 `testenv::ENV_LOCK`。CI `cargo test --locked` 红则滚动 tag `dev` 不更新 | 2026-09-07 |
| M050 | pwsh 启动 `Invoke-Expression` 报空串；金库实际有 token | D14 把 `~/.ohmyagents` 迁到 `~/.oma` 后 PATH 旧 oma 仍读空根，`env` 吐空；pwsh 块无条件 IEX。Windows `inject zsh` 只写 `%USERPROFILE%\.zshrc`，WSL 真正读 `/home/ray/.zshrc` 且 PATH 无 oma | 更新 PATH oma；空 vault 出 noop 注释；pwsh 空串跳过 IEX；Windows inject zsh/bash 另写 WSL 家目录，块内回退 `oma.exe` | 2026-09-07 |
| M052 | CI mirror 首跑把解包目录裸二进制也传进 R2 段（9 对象而非 6），加 `--delete-excluded` 后二跑仍未删 | 两个叠加：dist/ 产物含解包目录未加白名单过滤；`--delete-excluded` 是 sync 语义旗标，`rclone copy` 永不删目的端 | 段同步用 `rclone sync`（不是 copy）加 `--include` 白名单加 `--delete-excluded`；传前想清楚「段内容 = 源内容」是 sync 语义 | 2026-09-08 |
| M053 | Git Bash 下 cargo 链接炸两连：`missing operand after '\377\376'`（GNU link）与 LNK1181 找不到 advapi32.lib（MSVC link 拿不到库路径） | 双层坑：`/usr/bin/link.exe`（coreutils）在 PATH 前段 shadow MSVC link；修正 PATH 后 LIB/INCLUDE 用 Windows 分号形又被 MSYS2 按 POSIX 路径单转换（盘符冒号切段），link 仍拿不到库 | cargo 调用前缀三件套：PATH 前插 MSVC `bin/Hostx64/x64`，LIB 与 INCLUDE 用 POSIX 形冒号分隔（`/d/...:/c/Program Files (x86)/...`，让 MSYS2 转）；含 SDK um/ucrt 加 MSVC lib，INCLUDE 加 shared/winrt | 2026-09-08 |
| M054 | 经 Bash 工具传 python heredoc，脚本里的 `\\u` 反斜杠序列被工具参数的 JSON 编码吃一层（python 实收 `\u`）：SyntaxError truncated \uXXXX 或 re.error incomplete escape；同会话同型连踩，另两处把 `r#"`…`"#;` 定界符里的 `#` 手滑写成反斜杠（上下文反斜杠密度过高诱发） | heredoc 引号定界只保证 bash 层不展开，不覆盖工具入参的 JSON 编码层：`\\` 在 JSON 里就是一个字面反斜杠 | heredoc python 避免反斜杠序列：转义用 chr() 或字符串拼接、定界符用字面 `'#'` 变量；正则能换字符串 find 就换；必须用反斜杠时按四反斜杠预期并先 repr() 自检模式；Rust 源内 `\u{...}` 常量改用 Edit 工具直改（参数逐字落盘不经再编码） | 2026-09-09 |
