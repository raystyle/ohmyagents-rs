# P0031：D10 G005 存量字符清理

> 2026-09-07 当日闭环。缘起：G005 封闭清单只减不增，存量清理排队。用户点名启动。diary/proven 仍 SKIP_DIRS 不回改。

## 方案

1. **复测**：掩豁免区、跳过 diary/proven。2026-09-02 量化 3671 含归档；现 SKIP_DIRS 外 555 处（DASH 476、ARROW 72、FULLWIDTH 6、EMOJI 1），清单 46 文件。
2. **FULLWIDTH / EMOJI**：全角加号改半角；TUI 字形举证放行内代码。
3. **ARROW**：U+2192 / U+2194 按语义改「然后 / 对应 / 互转 / 变成」，禁止正文裸 `->`。
4. **DASH**：`——` 改冒号；文献范围 `L71–80` 改半角 `L71-80`；表空单元格改 `-`。误把 `——target` 当成破折号处改回 pane target。
5. **收口**：文件清零即删清单行；清单空则删 `.tools\md-char-allow.txt`；mdcharlint 零容忍、无豁免尾注。

## 验收

- SKIP_DIRS 外四类禁字 0。
- `uv run --script .tools\mdcharlint.py` 退出 0，输出 `通过：未发现违规字符`（无封闭清单尾注）。
- rumdl 与 md-ref / md-heading 触碰文件过。

## 实施过程与经验

- 09-02 的 3671 含 diary/proven；SKIP_DIRS 后工作量是 555，先复测再动手。
- 批量替换必须走与 mdcharlint 相同的掩码，否则行内代码里的合法字符会被误伤；`——target`（本意 `--target`）被当成破折号，要按语义回改。
- 第三方 wasm README 的英文 em dash 不宜改成全角冒号，用分号或逗号。

## 钩子

- 门禁：`.tools\mdcharlint.py`；细则 G005。
- 遗留：diary 与 proven 历史归档仍 SKIP，不回改。
