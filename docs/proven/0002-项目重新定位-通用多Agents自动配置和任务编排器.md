# 项目重新定位：通用多Agents自动配置和任务编排器

- 状态：已完成（定位文档已改，实现仍按阶段推进）
- 日期：2026-08-29
- 关联：`docs\references\项目定位-通用多Agents自动配置和任务编排器.md`；方案 0001 降为首期切面

## 背景与问题

方案 0001 把产品写成「一条 CLI 操作四路会话、浏览器可选镜像」。用户定调更宽：本仓是**通用多 Agents 自动配置和任务编排器**。四路会话和网页镜像仍是实现手段，不再是定位本身。

## 目标与非目标

- 目标：权威文档（AGENTS、README、项目定位、CHANGELOG、ROADMAP、清单）改成新定位；0001 标明为首期切面。
- 非目标：本轮不改研究五篇的技术结论；不开 Cargo。

## 方案

定位三句话：自动配置项目级 agent 文件；编排多路任务；agent 集合可扩展，当前默认四家。

0001 的 CLI、2x2、项目级 hook、`--no-web` 仍然有效，作为编排器的第一期能力，不是产品天花板。

## 备选方案

维持「只做四路会话工具」：否，与用户定调冲突。

## 实施步骤

1. 新写项目定位 references，删旧定位文件
2. 改 AGENTS / README / CHANGELOG / ROADMAP / 全量清单
3. 本方案归档进 history

## 风险与回滚

定位变宽后容易把 evo-harness 整套 graph 塞进来。边界仍写清：不替代环境总台、不用 herdr 当宿主。回滚即还原本方案提交。

## 验收标准

- 根目录与 references 不再把「一条 CLI 操作四路」写成现役定位
- 文件名与标题对齐：`项目定位-通用多Agents自动配置和任务编排器.md`
