# P0033：D12 任务目录孤儿收敛

> 2026-09-07 当日闭环。缘起：根下 `.ohmyagents/t006/` 不属于 `tasks/` 协议目录。用户点名收敛。`.ohmyagents/` 已被 gitignore。

## 方案

1. **对账**：`tasks/t006/` 是第一轮 review（FINDINGS=13，HEAD=064ca87，有 DONE）。孤儿 `output.md` 是 t008 第二轮草稿（FINDINGS=12，HEAD=3652180），与 `tasks/t008/output.md` 只差两处措辞。
2. **删除**：以 t008 目录为正本，删 `.ohmyagents/t006/`。不动 `tasks/t006/` 与 `tasks/t008/`。
3. **防再发**：`task_protocol_note` 把路径写成显式 `.ohmyagents/tasks/{id}/`，单测拒绝 `.ohmyagents/t001/` 这种缺段形态。现行尾注本就靠命名参数注入 `tasks/`，显式写法避免以后改回漏段。

## 验收

- `.ohmyagents/t006/` 不在；`tasks/t006/` 与 `tasks/t008/` 产物仍在。
- `cargo test --lib task` 含 `protocol_note_paths_stay_under_tasks_dir`。

## 实施过程与经验

- 孤儿不是当前协议漏 `tasks/` 的回归：`task_new_inner` 已用 `id = format!("tasks/{id}")`。内容标题写明 t008，更像第二轮 agent 按上一轮编号落盘。
- 命名参数注入 `tasks/` 正确但易被「简化」成 `.ohmyagents/{id}/`。路径写死在字符串里更安全。

## 钩子

- 代码：`src\task.rs` `task_protocol_note`。
- 遗留：不扫其它 `.ohmyagents/tNNN/`；本机仅此一份。
