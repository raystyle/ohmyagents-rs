# 2026-09-03 仓库清理与工具清单对账

> 小事日：清盘上构建垃圾与误提交文件，对账 `.tools` 清单。无产品代码变更。

## 做了什么

- 盘上清理：删 `target-review`、`target-review2`、`target-review3`、`target-review4`、`target-test` 五个 `CARGO_TARGET_DIR` 临时构建目录（review 轮换与测试期的产物，`.gitignore` 的 `target-review*/` 模式就是为它们写的）与 `.serve-fg.log` 前台调试日志。
- git 清理：`.oma-p10.txt` 是 P0010 开发期误提交的临时路径标记（内容仅一行 temp 目录路径），`git rm` 出仓。
- 清单对账：`.tools` 的 `review-round.py` 与 `share-view-probe.py` 已进 git 但未登记 README 清单（违反自述规则），补两行登记；两脚本有完整 docstring 与复用价值（轮换接力 review 工作流、web-share spectator 探针），修登记而非删。

## 学到什么

- 「无用文件」要分三类处理：盘上垃圾（直接删）、误提交临时物（`git rm` 出仓）、规范违例（修登记而非删文件）。git 工作树干净不等于仓库干净，ignored 区才是垃圾聚集地。
- 数据与缓存不进清理范围：`.ohmyagents` 的 tasks 与 reviews 是 oma 功能数据与历史轨迹；`docs/web/share-src` 的 dist 与 node_modules 是前端构建缓存，删了下次构建全部重来。

## 自省

- 根下 `.ohmyagents/t006/` 与 `tasks/t006/` 内容不同，属早期 task 布局遗留，未动：不能确定哪个是有效历史产物，宁可保留不误删。

## 钩子

- 验证全绿：rumdl 97 文件无问题、md-ref-scan 98 文件 0 断链、md-heading-scan 0 括号标题、mdcharlint 通过。
- 待接：无新立项；早期 task 布局孤儿目录（根下 `.ohmyagents/t006/`）是否收敛进 `tasks/` 待定，动前先核对 t006 两轮产物归属。
