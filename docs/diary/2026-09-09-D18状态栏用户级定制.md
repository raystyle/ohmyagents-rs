# 2026-09-09 D18 状态栏用户级定制

## 钩子

- `4f296b9` chore: rustfmt 存量漂移清零（D17 提交后七文件）
- `b376e36` docs: D18 立项四原语流转
- `975b7f5` feat: D18 切片 1 状态栏脚本拆段拼装
- `a5bb7e9` feat: D18 切片 2 状态栏段落开关
- `b1c2275` feat: D18 切片 3 模板变量与图标映射
- `8401b35` feat: D18 切片 4 整脚本替换
- `1176184` feat: D18 切片 5 codex 内置项子集
- `a6b4994` docs: README 精练为人类面向
- `ac1d429` docs: D18 归档 P0039 与文档同步
- `9d2ae0d` chore: v0.3.0 封版（版本号 0.2.0 升 0.3.0）
- tag `v0.3.0` 正式 release 六资产齐
- 封版与 D41 关账文档随后补一笔 docs

## 跨仓回执（D41）

- herdr 通知 ohmycloud claude（`w6:p1`）：mirror job 上线、oma/dev 六资产 04:59Z 自动到货、stable 段随下一版 v* tag。
- ohmycloud 当日验收回执（issuecomment-5598606699，08:15Z）：**dev 段 PASS**（三方对账：域面整件实算 sha = 域面边车 = GitHub release digest 逐字一致，尺寸对齐）；提醒边车是 GitHub 风格 `<sha>␣␣<filename>`，消费方解析要剥文件名段；stable 段挂账等下一版 v* tag 补验后 D41 整行关账。
- v0.3.0 tag 后 stable 段首供（三压缩包加边车 HEAD 200），ohmycloud 自主补验回执（issuecomment-5599216966，08:58Z）：**stable 段 PASS**（三方对账同口径，digest 52631254 / 1524f2ec / fc5 开头三件）。D41 双段全绿整行关账。
- 本仓 PRD D41 / GOAL 挂账已同步该口径。

## 封版（v0.3.0）

- D18 批 13 笔推 origin（`2c5412a..76160bd`），CI 两轮绿（dev 滚入 D18 产物）；版本号升 0.3.0 提交（`9d2ae0d`）再推再绿；tag `v0.3.0` 推 origin 触发正式 release，六资产齐（三平台 zip / tar.gz 加 .sha256 边车）；mirror job 随 tag 首次填充 oma/stable 段。
- 域面抽验：oma/stable 三边车 200，win 边车 1524f2ec 与 ohmycloud 对账 digest 一致。

## D13 实收（lan-mac）

- ssh lan-mac（Tailscale）：部署位 `~/.cargo/bin/oma` 是编排纪元化石（命令面还有 spawn/serve，无 self 无 --version），无法走 self update；经镜像 stable 段直装 v0.3.0：下载 oma/stable aarch64 资产、边车 sha256 校验过（52631254 = ohmycloud 对账 digest，三方一致；mirror 第二端点消费实证）、tar 解包 install 原位替换，旧件备份 `~/.oma/oma-fossil-backup`。
- 实收：`oma --version` = oma 0.3.0；`oma agents` 四家 installed=4（darwin 探测正常）；`oma agents statusline --example` 定制模板面绿。PRD D13 转已交付，TODO 队列传空。

## 流水

1. **开仓三件**：D17 四笔推 origin（`e01db5f..2c5412a`），CI dev-release 全绿含 mirror job，oma/dev 段资产 2026-09-09T04:59Z 六件滚新；D13 前置齐，待 mac 侧 `oma self update` 实收（用户侧动作）；D18 追问链三问经 AskUserQuestion 三裁：粒度三层全要（段落开关加整脚本替换加模板变量）、缺省键回落内嵌、codex 一并支持，裁「立项开工」。
2. **切片 1 拆段等价**：`STATUSLINE_PS1` 单体 const 按 18 处边界拆 HEAD / COMMON / PROBE / 14 段块 / TAIL，拼装器 `assemble_statusline_ps1` 按段序重组；COMMON（rev-parse）与 PROBE（projKind 探测）按消费方门控拼入。等价判据双保险：与拆段前基线四路 mock（claude / grok × 空 / 全 JSON）pwsh 直跑输出逐字节一致；PROBE 前移属纯位置移动无语义影响。
3. **切片 2 段落开关**：`~/.oma/statusline.toml` 顶层 `segments` 键（段 id 数组即全量序），键缺省回落内嵌 14 段默认序；坏文件硬错带文件出处；`--example` 打印带注释模板（对齐 providers 先例）。
4. **切片 3 模板与图标**：段文本构建改 `ApplyFmt (Tmpl 段) @{...}` 形，`[template]`（含 `context-ascii` 的 grok 结构差异项）与 `[icons]` 键级回落烘焙进 `$slTmpl` / `$slIcon`（单引号加倍转义，用户串无法越出字面量，注入不成立）；未知模板或图标键硬错。图标码位逐字对齐拆段前脚本（python 实测抽取：package=F03D7、rust=F1617、ts=F06E6，与预猜全不同，查对才落）。默认渲染与切片 1 基线四路 mock 再比对一致。
5. **切片 4 整脚本替换**：`--script <路径>` 拷自备脚本到部署位（agent 配置命令行不动），`<脚本>.custom` marker 记源路径，此后无 `--script` 重跑跳过内嵌覆盖；`--builtin` 还原；kv 面加 `statusline.custom` / `statusline.script`。调用契约进 R002：首参 agent 名、stdin 喂 agent JSON、stdout 单行、kimi 300ms。
6. **切片 5 codex 子集**：`[codex] items` 原样透传渲染 `[tui].status_line`（codex 对未知 id 静默跳过，oma 不拦）；`--example` 扩全量三层文档。
7. **测试与验收**：145 单元加 24 集成全绿（新增拼装器、配置解析、烘焙合并、注入防逃逸、marker 语义、codex 子集与 pwsh 闸门行为判据）；本机 `oma agents statusline` 部署新脚本、`oma agents verify` 四家全绿、部署脚本 mock 直跑渲染正常。测试封闭性两修：子进程摘 `OMA_AGENT` / `OHMYAGENTS_STATE_FILE`（宿主 agent 会话泄漏）、cwd 钉 scratch（活仓 `.oma/state` 泄漏状态）。
8. **README 精练**（用户插单）：人类面向三段重写：三平台预编译安装（资产布局 tar / zip 实测后写对 Windows 解压后的 Move-Item）、源码安装、使用示例按部署 / 状态栏 / token / 诊断 / 检索 / 自维护分组；去 D / P 编号与验收机口径。
9. **文档八面**：R002 加状态栏用户级定制行（三层键语义、门控、契约）、COMMAND_MAP 行更新重跑 init 再生四家 SKILL、R011 冻结面注 statusline marker 只增、AGENTS 路由行、INDEX 代码表与错误速查段（M054）、CHANGELOG 里程碑 2026-09-09、PLAN / TODO / GOAL / PRD 流转。
10. **门禁与坑**：四门禁全绿（rumdl 一处昨日 diary 双空格微修、README 三处标题去括号、PLAN 切片 1 标题去括号）；M054 记档：经 Bash 工具传 python heredoc 的反斜杠序列被 JSON 编码吃一层（`\\u` 落地成 `\u`），同会话同型连踩（定界符 `#` 两写反斜杠），处置为 heredoc 内避免反斜杠序列（chr / 拼接 / 字面量）加 Rust 源常量改 Edit 工具直改。
