# github研究法与双通道规则

> 2026-08-31。编码经验落地方法二（GitHub 搜索研究法）实证，两法齐后按用户框架升规则。

## 做了什么

- 读 `D:\sourcecode\CoreSkills\skills\investigator\` 的 SKILL.md 与 gh 速查表，明确分工：investigator 是代码符号级调查链，本方法是选型研究级上游，clone 后衔接其六步。
- 本机实证：`gh search repos` stars 排序一屏分辨流行与活跃（tab-rs 684 星 2023 停更对 limux 546 星活跃）；`created:>2026-01-01` 新秀筛选（头名 40873 星，herdr 33710 在列）；`gh repo view` 质量信号（rmux 2606 星、license null 与 crates 元数据不一致的真实信号）；releases 节奏（rmux 两周一版）；`gh search code` 对 rmux-sdk 空（新库无第三方用法，生态信号）；`--filter=blob:none --no-checkout` 轻量克隆 1.9MB 全历史。
- 用户插话要求验证文档搜索：gh 2.97.0 无 docs 子命令；正解 `gh search code "<词> extension:md"` 内联（跨仓扫 md 实测命中 fzf CHANGELOG 等），`--filename "*.md"` 通配空（陷阱复现）；官方文档定点 `--repo github/docs`（webhooks 命中 content 源文件）。
- 两法齐六态完备，按既定框架升规则：新建 `docs\guide\选型研究细则-cratesio与github双通道.md`（两法工作流、稳度四信号、坑速查十行）；AGENTS 写 Rust 规则挂细则引用。

## 踩坑

- qualifier 混独立 flag 报「None of the search qualifiers apply」：created 等限定词须与关键词同在引号内。
- view 无 openIssues 字段：取 `issues.totalCount`；search 与 view 星数字段名不同。

## 改了什么

- 新增研究一篇与 guide 细则一篇；AGENTS、全量清单、TODO 同步。
