# 2026-09-01-和解式编排与资源包化

> 一天一篇：当天全部工作的总结与自省。

## 今天做了什么

1. **P0023 看板资源包化**：build.rs（本仓首个）打 tar.gz 嵌二进制，首启按内容指纹释放 `~/.ohmyagents/web/<sha8>/`，serve 从释放位托管——单 exe 即完整产品（S022 三路线裁决：嵌入归档加释放，零新运行时依赖）。
2. **S023 进程树实测**：用户给定框架，本机活会话 PPID 链加源码双向核实，**纠偏三处**——daemon 运行形态是 `rmux.exe --__internal-daemon`（非 rmux-daemon.exe）、conhost 与 pane 进程是兄弟非隔层、pane 不必有 shell 层（oma agent 裸挂 daemon）。
3. **P0024 和解式编排**：用户定调「命令不关注窗格只关注 agent 实例」——spawn 升级三态（新开/附加/死路重开），`oma respawn` 强制单路重开（kill-pane 单窗格）；P0019 的死路缺口补上。
4. 看板品牌化收尾：去 logo、去 SHARE 字样、去全部 rmux.io 外链、目录 share-fe 改 kanban。
5. **P0025 serve 守护化**：用户定调「serve 类命令应直接退到后台 daemon」——`serve start` 即调即退（DETACHED 孤儿化）、`DELETE /shutdown` 协议化停机端点（rmux kill-server 同构）；最硬一坑是 tasklist 在 Job Object 内管道死锁，改 FFI OpenProcess 探活。
6. **G004 经验沉淀细则**（用户定调强规则）：成功经验分 proven（成功 plan 沉淀）与 references（研究实证做法 + 多次错误升格的正确工作流）双链、错误经验 mistakes 当场记加二犯升格；挂 AGENTS 工作节奏第 5 条。
7. **M035 记档**：python `str.replace` 吃 `docs\references` 里 `\r` 转义劈行——记档修复过程又踩同型两次，最终逐字节 repr 检查才清净。
8. **README 三段重写**：项目介绍 / 安装部署 / 完整命令示例（含典型用法）。
9. **次轮纠偏**（跨 compact 续轮）：核对三原语发现 `serve_stop` 实际只有 taskkill（P0025 验收超写）——补 `DELETE /shutdown` 优先 + 轮询退出 + 超时强杀兜底，实测日志见 draining；README/AGENTS 对齐 start/stop/status 形态；三原语对齐（PLAN 从 P0005 旧计划刷新）。

## 关键裁决

- 绑定链完整口径：服务 label 探测不重开 → 会话 reuse_only → 窗格 manifest pane_id → 死路 reconcile 重开 → 强制 respawn 单路。「初始检测互斥，操作绑定已开实例」全程成立。
- 资源释放的单位是内容指纹不是版本号；清理（旧指纹）必须在复用与重释放两条路径都做（单测边界用例抓获）。
- 排查 rmux 进程按 `--__internal-daemon` 参数找，不按进程名。
- 验收标 `[实证]` 前必须核对**命令面**真消费了该路径——端点存在不等于命令接线（P0025 首版 stop 只有 taskkill、手册却写协议化，就是部件验证过、链路没验证）。
- Job Object 宿主环境里一切 `.output()` 式子进程等待都是雷（tasklist 死锁），探活优先 FFI。

## 自省

用户两次框架级输入（进程树描述、三态语义）都先被实测/代码核对再采纳——本轮各纠偏三处与一处语义升级，印证「用户给框架、实证定细节」的协作节奏。死路缺口从发现（P0019）到补上（P0024）隔了一轮，但补的形态比当时设想的 respawn-only 更完整（和解式顺带覆盖附加语义）。
