# 2026-08-31-研究体系与POC全绿

> 一天一篇：当天全部工作的总结与自省。工作细节在方案与研究文档，本篇只留概貌、裁决与得失。

## 今天做了什么

> 概貌。

1. **paste POC 跑绿**（方案 P0005 高项）：全 CLI `-L` label 三段式，中文粘贴实证；排掉三个硬坑（`-S` 无条件拒绝、SDK `cmd()` Windows 不可用、WMI 就绪延迟）。
2. **测试规范立起来**：书、2026 社区综述、microsoft/rust-guidelines 三源对照，沉淀为 AGENTS 写测试规则与测试标准细则；顺手把操作规则重组为两组九条、Drive 细则分流进意图路由。
3. **编码经验与选型研究法落地**：ponytail 懒人阶梯研究；两个落地方法（crates.io 双通道、GitHub 研究法）全部本机实证后升为选型研究细则；第三方 CLI 四装三汰一留 cratesinfo。
4. **知识库结构大整编**：guide 与 references 分职（元规范对开发测试参考）、六篇研究合并四篇、win-rmux 吸收篇压缩、复核篇归档、日记从六篇并一篇。
5. **P0005 Windows 范围全表绿**：下午连拿五城——locate（pid 反查错位 throw）、stream（Oldest 回放 Now 直播）、state（terminal_state 分类 Quiet 不当 idle）、init（四家 hook/skill 项目级部署幂等不改家目录）、negatives（C-c Codex 守卫、只杀本 session）。12 个 example 全部退出 0。
6. **hook 研究一手化**（S015）：Claude 官方 hooks reference（llms.txt 定位）+ openai/codex、xai-org/grok-build、MoonshotAI/kimi-code 三仓浅克隆源码，替代二手转述；订正 Claude PermissionRequest 旧口径、关闭 Kimi 项目级悬案。

## 关键裁决

> 当天拍板的口径。

- 术语只是意图，意图对应方法：冒烟/集成/回归/验收不再考据定义，直接映射方法。
- 测试期望值必须来自独立来源；重言式断言（AI 高发）进禁止清单。
- trust.skill 只对 skills-dir plugin 形态报，普通 skills 归 trust.project（官方口径，反转旧裁决）。
- 研究对 references 分工：研究管原型过程与为什么（六态）；references 管开发测试中要做什么怎么做（六态溯源）；MISTAKES 管出错怎么纠。
- 一天一篇日记，写总结与自省，不写流水。

## 追记：结构化工具链收口

ast-grep（ohmyenv 0.45.1）三场景实证入 INDEX：outline 符号表、按名结构定位、签名表；两坑入 M024/M025。mq（ohmyenv 0.8.4，harehare，jq 风格 markdown 查询）实证：`-F grep` 跨文件按节定位、`-A section::section` 抽整节（全仓 8 篇「关键结论」一次拉出）、`-F json` 结构输出；坑入 M026。用户定调收口：INDEX 只做定位，搜索与分析方法（rg/mq/ast-grep 配合 INDEX）进 AGENTS 资源索引节。

## 追记：.tools 项目工具目录

建 `.tools`（进 git）归档自定义临时脚本，首发两个工具由当日验证过的内联脚本正式化：`md-ref-scan.py` 断链扫描（配豁免清单，47 文件 0 断链 exit 0，可进门禁）与 `md-replace.py` 字面替换（治 M023 sed 坑）。Python 统一 PEP 723 加 `uv run --script`（对齐 ohmypwsh）。AGENTS 加规则 10「写临时脚本时」与意图路由「项目工具」条；INDEX 登记代码位置。

## 追记：循环自迭代规则确立

AGENTS 新增工作节奏规则 3「发现问题时（循环自迭代）」：定位（搜 INDEX 查覆盖）到归类（文档错修文档、规则缺补规则、知识缺落研究、出错记 mistakes、做法沉 references）到修正（改源头同步下游）到验证（rumdl 加 md-ref-scan 加对账）到提交的五步闭环；G003 补第五节展开。顺手修掉 G003 标题双前缀漏网与规则 11 缩进——该漏网本身就是自修正循环的第一次实跑。

## 追记：POC 五连绿与全表收官

下午按 S003 各 POC 节的验证口径逐件实现：locate 走批量 CIM 反查加 send 前守卫；stream 验出 Oldest 首块 sequence=0、Now 不回放（marker 子串自命中坑记 M027）；state 落最小 `detect_terminal_state` 分类器（password 优先、col-0 也命中，中文缺口单测锁死）；init 先做 S015 一手研究再按矩阵部署（deploy 模块 merge-only，drop-stale 初版把当前 exe 也删了、二次部署必重写，修成只认「嵌不同 oma 路径」）；negatives 用兄弟会话实证只杀本 session。三个新坑入 M027/M028，deploy 坑留 P0005 经验节。

## 追记：研究方法一手化转向

用户定调：四家 agent 除 Claude Code 外都有官方源码，用 gh 定位仓库浅克隆读源码；Claude Code 官方文档有 llms.txt 可定位。照此办成 S015：Claude 抓出 PermissionRequest 程序化裁决（推翻本仓旧口径「Claude 无此事件」）；Codex 拿到 commandWindows 与 hooks.state.trusted_hash 一手形状；Grok 官方仓竟是 Claude 同构且无 PermissionRequest；Kimi 项目级 local.toml 只收 workspace.additional_dir（悬案关闭）。中途误克隆 Python 版 kimi-cli，用户当场纠正为 TS 版 kimi-code——选仓先确认技术栈与本机安装一致。另踩 rg `-rln` 实为 `-r ln` 替换标志的坑（M028）。mistakes 迭代规则按用户定调补「同根因可合并聚合归档」。

## 追记：P0006 产品命令闭环当日达成

POC 全表绿后按方案直落产品层（P0006 立项到验收同日完成）：`oma spawn/status/send/cleanup`。关键取舍是「可重连身份」——项目路径哈希出稳定会话名与端点，pane 用 daemon 稳定 id 记进 `.ohmyagents\session.json` 清单，跨 CLI 调用不漂移；status 把 S009 四层权威链（pid、locate、1b 分类、hook 沉默）首次产品化。tests/cli.rs 以 assert_cmd 起步 5 例，全套 38 测过。教训：orch 初稿臆造了三个不存在的 SDK 方法名（`spawn_argv`/`panes()`/`find_pane_by_text`），编译期全被打回——写 SDK 调用前先 rg 签名，与 M024/M025 同族，值得聚合成一条 mistakes（下次合并时并入 M104 或 M107）。

## 追记：P0007 label 桥与多行粘贴同日达成

「继续」第二刀解决多行任务发送。堵点是传输面分裂：三段式粘贴只能走 CLI `-L`（Windows 拒 `-S`），label pipe 名带随机 salt SDK 连不上，而产品会话在 SDK 端点上。破局是先做小验证再立项——`display-message -p '#{socket_path}'` 能打出 label daemon 实际 pipe 全名，SDK 拿这个名直连成功（poc-label-bridge），于是产品端点整体迁 label 命名空间：CLI 起 daemon、SDK 干细活、paste 走 CLI，一个 daemon 双传输面。顺手做出自愈（manifest pipe 失效按 label 重查回写）。新坑 M029：rmux `-t` 是前缀匹配，boot 会话名 `oma-<slug>-boot` 被产品名前缀命中误报「已存在」——会话名之间不得互为前缀。验收含中文两行粘贴（第二行执行输出 confirm 可见）。

## 追记：标题括号规范全库整改

用户点名 AGENTS 规则 3 标题带括号——G001 三禁（不带括号、不喊口号、不用破折号）立了多日，规则文档自己先违反，且 AGENTS 规则 8 转述把三禁粘成「括号口号」弱化了禁令。自迭代走全五步：定位（读 G001 确认禁令边界是一切括号；全库扫出真标题违规约 49 处，代码块注释不计）、修正（机械 37 处派子代理批量迁 `>` 引用行，核心规则文件 12 处自己改）、堵源（`.tools\md-heading-scan.py` 进验证三件套；G001 检查表与 AGENTS 验证步挂链；规则 8 措辞回对齐原文）、记 M030。这轮再次证明：只靠人工自检的规范必然被自己违反，规范落地必须配机检门禁。

## 追记：R008 Python 选型细则

用户提供《Python 库选择与开发稳定指南》作项目工具经验增量。按 R005 先例先实证再进规则：PyPI JSON API、simple index（PEP 691）、pypistats recent（httpx 月下载 8 亿，即 Python 版 recent_downloads 信号）本机全通，`pip search` 失效按官方口径标经验。落 `R008-项目工具Python库选型细则-pypi与uv.md`，与 R005 分工（Rust 产品依赖对 py 项目工具），挂 AGENTS 规则 11、意图路由与 `.tools\README`。顺手又扫出四处双前缀标题漏网（G002/R004/R006/R007）——当年 REVIEW 修了一批漏了一批，说明「批量修完要跑全库同类扫描」而不是逐个见一个修一个。

## 自省

**做得对的**：素材一律先实证再进规则——今天多次被实证打脸（综述四工具三坏、`-S` 文案误导、反向依赖字段陷阱、Claude PermissionRequest 旧口径、kimi-cli 是 Python 版），若直接采信就污染了规则层。paste 排障坚持「撤坏层换平台已有面」而不是绕着修补，符合懒人阶梯第五档。研究整合先盘点后动刀，19 篇的交叉关系靠子代理全量分析定方案，没有凭印象合并。POC 连打坚持每件绿了立刻回填 S003/方案/TODO，收尾时全表状态与磁盘一致，验收零补作业。

**做得不足的**：日记上午失守（一天六篇流水，用户纠正后才归一篇）；下午守住了追记模式但文件名起窄了，收尾被迫改名一次。rg `-rln` 与 deploy 的 drop-stale 自删都是「写完没先想清谓词边界」的同型错——一个替换标志吞了 `-l -n`，一个 is_ours 包含自己；测试先行的价值在两处都被验证，state/init 的单测确实把坑拦在了 example 之前。研究 kimi 时先入为主挑了 star 更高的仓，没有先核对技术栈与本机安装一致，浪费一次克隆。

**明天注意**：动目录结构前先写一页四目录职能表再动手；日记文件名按全天主题起，收工只合并不改名；谓词类逻辑（守卫、去重、过滤）先写边界用例再写实现；跨仓研究先确认「本机装的是哪个发行版」再选仓。
