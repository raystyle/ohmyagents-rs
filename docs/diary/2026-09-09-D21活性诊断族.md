# 2026-09-09 D21 活性诊断族

## 钩子

- `a0cc055` feat: D21 oma diagnose 活性诊断族
- 收尾文档与 P0041 归档随后补一笔

## 流水

1. **需求与裁定**：ohmycloud D45 配套需求单 ohmyagents-rs#8（缓存探测与 agent 配置检测进 oma）；用户裁独立 `oma diagnose` 族与 doctor 零网络体检分家；PRD D21 登记四要点（ds 特判 auto-prefix、codex 默认 zy-gpt56sol-codex、矩阵 15 显式加 1 隐式、凭据读 agent 侧配置不建存储）。
2. **取证**：WSL 床读 D45 渲染后真实配置形态（claude env 带 BASE_URL 加 AUTH_TOKEN 加 MAX_THINKING_TOKENS 65536、codex model_providers.cpa 带 base_url /v1 加 auth.json OPENAI_API_KEY）；/v1/models 真名 16 别名（ds 线实名 ds-deepseekv4pro-claude 等）。
3. **实现**：`src\diagnose.rs` 网关发现三源（env 覆盖大于 claude env 大于 codex provider）加 cache 双连探测（claude 线 cache_control 加 codex 线 responses，verdict 五态 hit / write-only / auto-prefix / none / error）加 agents 检测（指向、在册、key 活性、thinking 上限表）。
4. **实收三坑**：缓存门槛差一（40 块 filler 1005 token 差 1 过 1024，96 块后 codex 线出 cached 1792）；网关两连异区只写不读（geo 漂移，催生三连取优读到即停）；上游路由瞬断（本机到 zy 10060 与 502、WSL 全通，error verdict 如实上报）。真网关三线判据全对表：codex hit、claude 显式写入、ds auto-prefix。
5. **测试与文档**：131 单元加 21 集成与四门禁全绿（新增 7 件单测）；R002 新 2.5 活性诊断节（原自维护节改 2.6）、COMMAND_MAP 加两行重跑 init 再生 SKILL、AGENTS 路由加一行、INDEX 加 diagnose 行改 install 行、README 加活性诊断节、CHANGELOG；P0041 归档。
6. **M054 五犯**：heredoc python 反斜杠路径再被 JSON 吃层，处置升格写入 P0041 经验（含反斜杠批改一律 Edit / Write）。
