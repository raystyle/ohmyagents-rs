# 2026-09-12：D29 更名 HST 破坏性迁移

> 当日：oma 更名 HST / hst / v0.6.0 七阶段执行与发版。方案与过程见 `docs\proven\P0046`；本篇记流水与自省。

## 做了什么

- D29 立项（用户方案全文一次给齐）加迁移计划呈报后动刀：crate hst-cli、命令面 hook 组三支与 statusline 一级化、`~/.hst` 根与启动 rename 迁移、HST_* env（旧 OMA_* 读并提示）、hst-* 资产加 oma stub 双挂、mirror hst/ 主段加 oma/ 兼容段、文档七面、迁移三用例与八条验收。
- 本机 dogfood：真迁移（`.oma` rename 加旧会话 shim 重建的双根并存实况被用例 2 预言）、heal 后四家注册收敛 hst-state、codex 无 bypass 落盘通道复验。
- ome 会话知识转递采纳核验：shim 实体缺失 heal 面（doctor shim-dead 探针 + init 必重落实体），记 P0046。

## 踩坑

- 仓运行时目录随根改名（`.oma` 至 `.hst`）后 rumdl 扫到运行时 md（review 报告等）：gitignore 补 `/.hst/`（rumdl respect-gitignore 即绿）。项目数据根 rename 把 gitignore 外的运行时产物搬了家：破坏性更名要过一遍「目录级引用面」不止代码。

## 自省

- 更名迁移的正确抽象是「heal = 重部署」：陈旧收敛判据天然跨名，没有为迁移写第二套改写器。
- 用户三条禁令（crate 仍叫 oma / 只换 PATH exe / 拼错 hts）各堵一类偷懒路径，方案先行的价值在把禁令变成验收条目。

## 明日候选

- 兼容别名一个小版本后删（agents statusline、oma stub、旧 env 读取）。
