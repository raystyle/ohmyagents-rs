# 2026-08-29 对照 ohmypwsh 建立文档骨架

> 当天做了什么（项目日记，按日期归档）。

## 工作记录

- **docs**：对照 `D:\ohmypwsh` 建立四段 AGENTS、三原语（goal/todo/plan）、history/research/references、mistakes、rumdl
- **docs**：方案 `0001-四路会话工具-CLI控制面与网页观察面.md`
- **docs**：五篇研究博客（此前已写）纳入全量清单
- **chore**：此前误跟 CoreSkills project skill 的元文件已撤；本轮按 ohmypwsh 重铺，不是同一套文件原样搬回
- **定位**：定为「通用多 Agents 自动配置和任务编排器」（方案 0002）；0001 降为首期切面
- **docs**：研究 yolo 与无阻塞启动（参数或配置）、doctor 诊断、任务指向；对照 ohmypwsh 0017
- **docs**：现有研究复核与扩展（公开文档 / GitHub / X），纠正 skip 键范围与 Codex hooks.json
- **docs**：rmux 状态判断与 hook 补充（层 0 存活、层 1 静默、层 2 hook 语义、层 3 任务指向）
- **docs**：吸收 win-rmux 既有 rmux 研究（全量：SKILL、14 篇 references、命令核验、`.rmux_tasks` hardened-guard；`D:\ohmyenv\rmux` 已不存在）
- **命名**：项目名仍是 ohmyagents；CLI 二进制定为 `oma`；数据目录仍是 `.ohmyagents`
- **docs**：研究 `rmux-sdk` 最佳实践并对齐 `oma` 原语；写出 POC-0..8 验证草稿（闸门仍是本机有 rmux）
- **feat**：方案 0003；`oma check` 本机装上 rmux 0.10.0，归档哈希与 `catalog/rmux.toml` 一致
- **定位**：方案 0004，Oh My Agents / 通用智能体多路复用任务编排器
- **规则**：Rust 优先用最流行、最稳定或已覆盖需求的 crate，最少代码接上
- **文档名**：对照 ohmypwsh，三原语改为 `GOAL.md` / `docs\PLAN.md` / `docs\TODO.md` / `docs\MISTAKES.md`
- **目标**：文档骨架目标归档；新目标为各功能部件 POC 验证原型（方案 0005）
- **feat**：项目级 yolo 与提示阻塞诊断 POC。`oma init --yolo` 写 `.claude` / `.codex` / `.kimi-code`；`oma doctor` 只读不 attach；`examples/poc-yolo-doctor.rs` 用临时目录验收。`--pretrust` 才写家目录信任库；本轮未对仓库 cwd 跑 init
- **docs**：学习研究 YouMind [d/QO6XcZnIybiBU1](https://youmind.com/d/QO6XcZnIybiBU1)（无头 ≠ YOLO，Claude/Grok/Kimi 三套答案）；报告 `docs\research\YouMind无头模式与YOLO免审批对照-Claude-Grok-Kimi.md`
- **docs**：核实信任门种类（目录 / hook / skill / MCP）；Claude 旧源 [codeaashu/claude-code](https://github.com/codeaashu/claude-code)；doctor 拆 `trust.project` / `trust.hooks` / `trust.mcp` / `trust.skill`
- **feat**：`oma agents` 检测本机 claude/codex/grok/kimi：PATH、`OMA_AGENT_PATH`、`OMA_*_BIN`、各家默认目录；Windows / Linux / macOS；doctor 的 `binary` 走同一探测
- **feat**：`oma hook` 作为各家 lifecycle hook 的 command：读 stdin 事件写 `.ohmyagents/state`，不连 rmux 管道
- **feat**：Windows 最小 pane POC 绿：`poc-endpoint` / `poc-session` / `poc-layout` / `poc-drive` / `poc-dialogs`。Job Object 下 `connect_or_start` 报 os error 5，改 WMI `Win32_Process.Create` 拉起 helper daemon 再 `connect()`。专用 pipe 名 `\\.\pipe\rmux-omapoc-<pid>-<tag>`。cleanup 只 `kill-session`。Linux/mac 跑测委托后续仓库
- **docs**：对齐 ohmypwsh 六态规则。AGENTS 操作规则第 10 条「写研究与测试文档时」；`docs\research\guide.md` 与文档标准细则同步；既有研究的关键结论补 `[实证]` / `[推断]` / `[经验]` / `[假设]` 标记
- **docs**：对齐 ohmypwsh 文档结构：方案只留 `docs\history\NNNN-*.md`；项目日记拆到 `docs\diary\YYYY-MM-DD-*.md`；方案模板补「实施过程与经验」
- **chore**：接 GitHub 初始仓库 `https://github.com/raystyle/OhMyAgents`（空私有仓）；`.gitignore` 排除本机 `.claude` / `.codex` / `.kimi-code` / rumdl 缓存；补 MIT LICENSE
