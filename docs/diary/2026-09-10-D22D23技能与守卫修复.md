# 2026-09-10 D22 D23 技能与守卫修复

## 钩子

- `fix: codex hooks command 字段 schema 必填`（M055）
- `fix: D23 secretguard 实值比对改完整 token 边界匹配`（#9）
- `feat: D22 oma skill 自适应技能生成`
- 收尾文档与 P0042 / P0043 归档随后补一笔

## 流水

1. **D22 自适应技能**：用户级 `~/.claude/skills/ohmyagents` 是旧手写版（挂 D20 已删面）；裁「命令自适应输出」：`oma skill` 从 clap 活命令树渲染 SKILL.md（frontmatter 按 Agent Skills 标准；命令表带 value_name 与旗标；新命令自动出现），`--write` 落用户级；位置参数 value_name 汉化；about 回落 Cargo 描述（顺带修过期 token 措辞）。
2. **D23 守卫误报**（#9）：实值比对裸子串改 `contains_complete_token` 边界匹配（两侧无 [A-Za-z0-9_.-] 类字符才算命中），杀模型别名连字符超集误报；真实密钥完整值仍 block；masked 带名[头4字符…长度]脱敏前缀。dogfood 自证：修 D23 时自己的 Edit 被在位 v0.3.0 hook 拦（注释里的真实别名撞 providers 值），改中性示例词绕过；误报对配置开发是日常阻塞级。
3. **M055 codex hooks schema**：verify codex 连红 no-state-file，用户贴错 `missing field command` 定位：deploy 的 per-OS 字段所有权在 Windows 侧只写 commandWindows，而 codex schema 里 command 必填（0.149.1 起解析失败整份 hooks 静默弃用）。修：Windows 侧新部署补 bare `oma hook --agent codex` 兜底；用户级残留 4 条同修；修后 codex exec 实跑 state 落盘实证。此前一直绿是被用户级双字段旧条目兜着（掩盖项目级单字段形态）。
4. **环境噪音剥离**：verify 本地仍红的第二因素 = 用户级 `models.json`（ohmycloud D45 模板活跃编辑中，mtime 当天分钟级；`supported_reasoning_levels` 字符串 vs 本机 codex 要 struct 的 schema 不匹配，codex 起会话前硬炸）属跨仓问题，已交底 ohmycloud 侧；CI 净环境不受影响。
5. **v0.4.0 封版**：D22 加 D23 加 M055 三件合并发版。
