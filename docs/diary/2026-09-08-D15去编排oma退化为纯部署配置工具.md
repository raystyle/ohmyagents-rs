# 2026-09-08 D15 去编排：oma 退化为纯部署配置工具

## 钩子

- `eeead00` feat: D15 去编排收窄为纯部署配置工具（代码面）
- `d289183` docs: D15 重新定位文档对齐与 P0035 归档
- `6428f25` docs: 2026-09-08 日记与 INDEX 日记节登记
- `3b16f6f` feat: D16 oma self update 加 OMA_MIRROR 镜像通道
- `9e172bb` docs: D16 镜像通道文档同步与 P0036 归档
- 本日记追加随后补一笔 docs

## 流水

- 用户三条裁定落定 D15：「还是 oma」「oma 退化为纯部署配置工具」「和 HOOK 和状态栏工具」。PRD 第 2 轮后即立项。
- 切片 1/2 委托 coder 子代理执行：11 个编排模块整删、rmux-sdk 等六依赖摘除、doctor 摘会话健康、tests 收窄，97 单元加 17 集成一次全绿。
- 子代理漏改 `deploy.rs` COMMAND_MAP：初跑 `oma init` 再生出的 SKILL 仍是旧编排命令图。改源头常量加重跑 init 后四家 SKILL 正确再生。教训已写进 P0035 经验节：收窄式重构要连生成物链路一起改。
- 门禁四件（rumdl / md-ref-scan / md-heading-scan / mdcharlint）全绿；顺手修了 09-07 diary 一处裸 URL（MD034，前日遗留红）与 P0004 的 R001 旧名断链。
- 未推远端。D13（mac `--version`）仍排队，等本批推送后的 CI 资产。

## 自省

- 委托拆解有效：代码面给子代理、文档面自己把握，上下文与责任都干净；但「生成物源头」（COMMAND_MAP 到 SKILL）这类跨层联动要在委托单里写明，否则默认漏。
- herdr skill（项目级 `.kimi-code/skills/herdr/`）同日应用户要求安装；与 D15 不冲突：oma 产品面不做编排，skill 只是本机工作流知识。

## 追加：D15 推送与 CI 转绿

`bc9e8a3..6428f25` 推 origin（首跑 send-pack sideband 断连，重试即过）。CI 最新一轮 2m50s 全绿，dev 滚动资产三平台 04:41Z 刷新带 sha256 边车；M049 红随 `31db518` 转绿。D13 门槛齐，待 mac 实收。

## 追加：ohmycloud 对账与 D16

- 对账五点回执（ohmycloud#6 Q1/Q5 加 D38① 挂账）：入镜点头；dev 与 stable 各回各段（ome #8/#9 教训，段名与通道同名不用 latest）；资产名即三元组确认（无 x86_64 mac）；rmux 真源裁归 ome catalog（D15 已删净本仓 rmux 持有）；oma 无镜像开关。
- 用户裁「要做」立 D16：OMA_MIRROR 镜像通道当日落地（update.rs 一节，五点设计见 P0036）。102 单元加 17 集成全绿；镜像网络分支未端到端，排队待 ohmycloud 播 oma/dev 段后实证。
- 自省：判新键跨源互认要先归一（GitHub `sha256:<hex>` 对 sha256sum 裸 hex），不归一换源必误更新；「网络失败回落、校验失败不回落」是镜像通道的安全分界。

## 追加：D16 镜像 e2e 实证

- 钩子：docs 笔「D16 镜像 e2e 实证注记流转（D38① 已交付）」。
- ohmycloud 播 oma/dev 段后来回执（六 URL 200、边车与 GitHub digest 逐字一致）。本机 Windows 一次性副本实跑三分支全绿：镜像下载校验替换、边车判新 already-latest、bogus 基址回落 GitHub。
- 坑：直跑 `oma self update` 会把工作二进制回盖成镜像上的旧资产（镜像滞后于本地源码）；一次性副本跑法写入 P0036 验收节。
- D38① 流转已交付（回执 issuecomment-5580376169）；rmux 真源归 ome catalog 随 ohmycloud#6 收档。

## 追加：尾巴修提交与 v0.1.0 封版

- 钩子：fix 笔「D16 镜像通道缓存击穿」`a0dc383`；docs 笔「D16 尾巴修加正式判据全过实录」`4fab9b6`；docs 笔「CHANGELOG 记 D16 与 v0.1.0 里程碑」`c50a097`。
- 用户裁：oma 整理清理后封首个正式版 v0.1.0（v* tag 触发正式 release，资产同 dev 形态带边车）；镜像分发与四端测试验收归 ohmycloud。D13 挂账等 mac 实收，D17 四问未裁不立项。
- 封版实证：v0.1.0 tag 推 origin，CI 两轮全绿（main 重滚 dev 带缓存击穿 1m54s、tag 出正式 release 1m37s）；`gh release view v0.1.0` 六资产齐、非 prerelease。四问回执：段名 oma/stable（通道同名先例）、四端议案认可、D17 未立项本轮按现有命令面验收、四件套判据逐项给出。
- D18 登记：用户问「能定制状态栏么」，答现状（内嵌 STATUSLINE_PS1 整文件重释放，手改必被重盖，定制只能 oma 生成时做）后用户「继续」，PRD 登记待澄清三问（定制粒度、合并优先级、codex 豁免）。TODO 不建行。
- 清理：项目根 `.ohmyagents/` 老目录删除（仅剩 D14 迁移前两件旧 state，`.oma/state` 已有四家新档；家目录无同名残留）；`.gitignore` 同步摘 `/.ohmyagents/` 行。

## 追加：D19 trace 六视图全量恢复

- 用户问 trace 为何不在（D15 归类观察面连坐删除，实则与 rmux 零耦合），裁「全量恢复」。PRD 登记 D19 当日闭环。
- 钩子：feat 笔「D19 trace 六视图全量恢复」`d7db60a`；docs 笔「D19 归档 P0037 与六处文档同步」`27af23c`。
- 恢复路径：trace.rs 自 `eeead00^` 原样带回 1387 行零适配；116 单元加 18 集成全绿；本仓真数据六视图冒烟实证（含本会话自身轨迹）。
- 坑：`--project .` 相对路径 slug 失配查 0 条，传绝对路径即正常（既有行为非回归，记 P0037 经验节）。

## 追加：D40 四端验收回执与 hook 判据修正

- ohmycloud 四端（win / wsl / lan-linux / lan-mac，v0.1.0 官方档镜像 stable 段装）验收：状态栏、yolo、token 注入加 D38③ 分发、hook 正负例全绿；唯一红是 hook state 不落盘。
- 根因自查：判据命令漏 `--agent` 且未说明 `.git` 祖先前置（`run_with_payload` 三闸）；实现无缺陷，本机带 `--agent` 复测 idle 加 working 落盘实证。记 M051，R002 hook 行补回退两前置条件。
- 验收矩阵更正（用户裁）：lan-linux 出列（该机无法安装 agent，已出结果降为参考不计判定）；D40 判定面 = 本机 win / wsl / lan-mac 三端，绿面与判红项按原回执不变、三端一致。修正后 hook 判据复跑范围同缩三端。
- v0.2.0 封版（用户裁「trace恢复后打新版本号」）：版本号 0.1.0 升 0.2.0（chore `1d763cf`），main 七笔连推（bb83e1c 至 1d763cf）加 tag 推 origin；CI 两轮绿，正式 release 六资产齐、非 prerelease。oma/stable 段待 ohmycloud 重播至 v0.2.0。
- D40 验收补充（oma doctor 三端跑毕）：win/wsl 各 29 项 ok（block 仅 trust.project 对话框态，环境非缺陷）；lan-mac 29 项 ok 加 warn 三家 hooks.form 未部署加 kimi 未登录加 grok 过期（均环境态）；lan-linux 参考端三家 binary 全 block 实锤用户出列裁定。判定面维持三端；唯一判红（hook state）已裁为判据不全非缺陷（M051），待三端按修正判据（git init 加 --agent）复跑收口。
