# 2026-09-09 D20 去 token 注入收窄

## 钩子

- `de7bd04` feat: D20 去注入收窄（删 secrets 五子命令、providers 别名簿、login 设备码引导、install/update 兼容层与安装机器；净删约三千行）
- 收尾文档与 P0040 归档随后补一笔

## 流水

1. **追问链**：用户裁定删 token 注入、收敛五功能（可用性诊断、hook、状态栏、trace、yolo）；PRD 登记 D20 后四问四裁：providers 一并删、login 删（doctor 登录态诊断保留）、install/update 兼容层顺带删、本地安装的先不动只改程序代码不要安装（profile 懒注入块与部署位不动、vault 不删）。
2. **代码面**：main.rs 删五命令面与五个 handler；lib.rs 摘四模块；删 secrets.rs / providers.rs / login.rs / catalog.rs 与 catalog\agents.toml；install.rs 从 995 行瘦至约 160 行（留 oma_home、managed_binaries / managed_version 存量探测、download_asset 供 self update）；secretguard 实值比对删 providers.toml 层只看环境变量（八层防误报其余不动）；`oma agents` 缺装 hint 改指 `ome install`；顺手修 statusline.rs 切片 4 遗留的重复行。
3. **测试**：单元 145 降 124、集成 24 降 21（三个 install 面与 providers 比对层测试随面删），全绿零 unused 警告。
4. **文档八面**：AGENTS（本质五功能、边界、四仓、路由删五行）、R001（本质边界加 D20 条）、R002（全表与 2.2/2.3 重排加移除面历史注记；修一处表被注记劈断）、R011（删排期句）、INDEX（代码表删五行加 D20 移除注记）、README（五功能重述删 token 节）、CHANGELOG 里程碑、COMMAND_MAP 删三行改描述重跑 init 再生 SKILL。
5. **坑**：M054 同型四犯（heredoc `\\` 被 JSON 吃层，`\r` 把路径劈成回车），处置升级写进 P0040：含反斜杠路径的 md 批改一律 Edit / Write 直改。
