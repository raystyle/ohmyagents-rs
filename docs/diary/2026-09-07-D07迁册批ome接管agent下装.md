# 2026-09-07：D07 迁册批，ome 接管 agent 下装

> 用户定调「ome 部分不管，只发 ISSUE，我们只清理自己项目」：oma 侧执行 ohmyagents#5 迁册批三件（install/update deprecated、agents.toml 冻结历史锚、doctor 四类归 agents 域），ome 侧部署滞后以 issue 交底，本仓不动 ome 仓。

## 做了什么

1. **对账现状**：读 ohmyagents#5（ome 源码已含 D07 切片 1 与 2，请 oma 配合三件）；核对门槛缺口：TODO 迁册批原登记「等 ome 仓 D07 切片 1 与 3 落地」，切片 3 待办（含 D08 CDN 兜底，等 ohmycloud D36）；本机实证 ome 部署位（`%LOCALAPPDATA%\Programs\ome`）与 catalog 同步层停 2026-09-02 旧版，`ome install claude` 报「未知工具」（源码新、部署旧）[实证： 当日实跑]。
2. **deprecated 提示（代码）**：`cmd_agents_install` / `cmd_agents_update` 入口 stderr 打 `oma.deprecated` 指向 `ome install`（不删命令、行为保留；stdout kv 与 json 面 R011 不动，update 注明通道语义由 ome 裁决）；clap 帮助与 COMMAND_MAP 同步；tests\cli.rs 两条集成测试（未知名加临时 `--root` 触网前快败，断 stderr 提示与非 0 退出）。
3. **agents.toml 冻结历史锚**：文件头注记数据权威转 ome `catalog\tools.toml` agent 四节，本文件不再随上游滚动；文件仍被 oma 加载（信任锚机制不动）。
4. **doctor 四类归 agents 域**：R002 doctor 行重排（登录态 / hook 形态 / 状态栏 / 会话健康归 agents 域；二进制在位与版本、token 诊断归 ome doctor）；AGENTS doctor 路由行同步；代码不动（ohmyagents#5「维持」口径，binary 在位探查保留作 spawn 前置）。
5. **文档与命令面同步**：AGENTS 边界段 D06 行改写为 D07 分工、四仓分工行补修正、三条路由行标注、`~/.ohmyagents` 行注记；R002 install / update 两行 deprecated 注；INDEX 三行（catalog、install.rs、P0029 归档）；重跑 `oma init` 四端 SKILL 再生（根 SKILL 对账队列项顺带收口）；四原语：PRD D07 流转已交付、GOAL 切 D07 并修正立项时起点锚点滞留 D06 的欠账、PLAN 重写、TODO 起新清单（mac --version 余量转队列）。
6. **跨仓交底**：ohmyenv-rs 发 issue（部署滞后实证与四家幂等验证请求）；ohmyagents#5 回填迁册批落地。归档 P0029。

## 自省

- **门槛裁定可以被执行日裁定推进**：TODO 登记的「等 ome 切片 3」是时点裁定，用户执行日改裁「先清自身面」；登记债的正确解法是改裁定记录（PRD 轮次注记）而不是静默提前。
- **deprecated 指向的目标要先实证可用性**：提示指向 `ome install` 前实跑发现本机 ome 旧版报未知工具；结论：提示照上（不删命令、行为保留，oma 发版节奏慢于 ome 部署），部署滞后单发 issue 交底，不在 oma 仓替 ome 动手。
- **PostToolUse md 守卫只报触碰文件全量**：新加行干净仍会被存量行触发提醒，先分辨「新欠账」与「G005 存量」再动手，只修新欠账（本次 GOAL 与 P0029 两处新加破折号当场改写，引用禁字本身也算违例）。

## 下一步

ohmyenv-rs issue 等 ome 部署含 D07 新版并实证四家幂等跳过；ohmyagents#5 可关（迁册批已交付，ome 侧其仓自跟切片 3 与 4）；队列见 TODO（G005 存量字符清理、状态栏工具链段、t006 孤儿目录、mac --version 一致性）。

## 追加：M044 Codex Windows-only 信任播种空表

用户报「review 项目 Codex hook 被改坏」。取证：ohmycloud / ohmyenv-rs / ohmypwsh 的 `.codex/hooks.json` 只有 `commandWindows`（P0027 字段所有权，纯 Windows 未跑过 WSL init），项目 `config.toml` `[hooks.state]` 空表；`codex_trust_entries` 只认 `command` 于是跳过播种。doctor 把用户 store 里 `~/.codex/hooks.json` leftover hash OR 进来，三仓都误报 `trust.hooks ok`。本仓因双侧字段都在所以哈希在、doctor 真绿，掩盖了受害面。

修正：播种与 merge 同口径认两个字段；hash 按本侧取值缺则回退；doctor 项目 hook 信任只读项目 `config.toml`。回归单测两条加 `deploys_merges` 断言 `trusted_hash`。M044 记档。

## 追加：M045 Codex 状态栏 command-argv 被静默跳空

用户报「状态栏也是」。取证：`~/.codex/config.toml` `[tui].status_line` 被 oma 写成 `["command","pwsh","-NoProfile","-File",脚本,"codex"]`。ohmypwsh S016（codex 0.148 源码）核实该键只收内置项 ID，无法解析的字符串静默跳过，整栏被跳空；S025 误写成「可外部命令」。doctor 因配置含 `oma-statusline` 仍报 ok。

修正：写入面改 S016 推荐内置项（`run-state` 等）加 `status_line_use_colors`；doctor 把 command-argv 标 warn。claude/kimi/grok 仍走 pwsh 脚本，不受影响。

## 追加：M046 Grok 状态栏字形显示错误；D08 澄清

用户报 Grok status line 显示错误，并裁定「ome只管软件部署；oma 要对 hook、状态栏和 agent 配置的检查」（纠正把 ome doctor 做成调用 oma 的误读）。取证：Grok `[ui.status_line] type=command` 配置合法（`grok inspect` 未报该节），脚本能跑，但 Nerd 私用区图标与 U+21E1 重复箭在 Grok pager 字体下变成替换符。

修正：ps1 对 grok 走 ASCII 安全路径（`agent:state`、`+N`/`-N`、跳过工具链子进程）；claude/kimi 仍用 Nerd。PRD D08 已澄清，检查面不收进 ome。

## 追加：M047 Grok hook ParserError 不停报错

用户报 HOOK 不停报错。取证：Grok harness 加载项目 `.claude/settings.json`；oma 写的 Claude exec 形态是 `command=oma` 加 `args=["hook","--agent","claude"]`，Grok 经 PowerShell 跑成 `"oma" hook`，引号字符串后跟 token 即 ParserError，每个 PreToolUse/PostToolUse/UserPromptSubmit 都弹。项目 `.grok/hooks` 的整行 `oma hook --agent grok` 本身是好的。

修正：Claude 与 Grok 都写一整条 `oma hook --agent <名>`，不再写 args；Windows 绝对路径加 `&`。重跑 `oma init` 改项目注册。

## 追加：M048 Grok 状态栏 os error 123

用户报 status line 还是错误，随后给出 `文件名、目录名或卷标语法不正确 os error 123`。取证：grok-build `xai-grok-pager/src/app/status_line/command.rs` 先 `Command::new(整串)`，只在 `NotFound`（Unix 另加 ENOEXEC）才回落 `shell_command_argv`。oma 写的 `pwsh -NoProfile -File "C:/...ps1" grok` 含引号与斜杠，Windows 报 ERROR_INVALID_NAME 123，不是 NotFound，回落永不发生，栏上画出 `[status line: could not start the script: ...]`。本机 `Command::new` 复现 123；`.cmd` 单路径 spawn 加 stdin JSON 实测出 ASCII 行。

修正：Windows 释放 `oma-statusline-grok.cmd` 并把 `[ui.status_line].command` 写成该路径；Unix 仍写 `pwsh -File`。改配置需重启 Grok。用户重启后实证栏正常（2026-09-07）。

## 追加：D08 立项 doctor 检查面补形态

用户报「继续 正常了」。盘点 `oma doctor` 本机全绿，但 M048 前同一检查也会报 ok（只认 command 含 `oma-statusline`）。D08 立项：doctor 补 Grok 状态栏三态与 JSON hook args 形态，堵住「写了配置仍 ok」。

## 追加：D09 oma 不管种子

用户裁定「oma不管种子 只管诊断、配置、hook、状态栏和编排」（纠正把 ohmycloud 镜像种子算进本仓 TODO）。PRD D09 已交付：AGENTS 边界五件事加不管种子；R001 四仓表与分发通道改 ome 消费镜像、ohmycloud 做种子，删「安装归本仓」旧裁决行。

## 追加：D08 归档 P0030

用户报「继续」。D08 验收口径已过（两单测、本机 doctor grok statusline ok、R002、四原语），归档 P0030；PRD D08 已交付；GOAL / PLAN / TODO 切空，下目标待立项。CHANGELOG 补 09-07 里程碑（D07 / D08 / D09）。

## 追加：排队四项入 PRD，D10 立项

用户点名四项。D10 G005 立项（复测 SKIP_DIRS 外 555 处，非 3671）；D11 状态栏 zig/go/cpp、D12 t006 孤儿、D13 mac --version 已澄清排队。D13 推远端仍待指示。D12 核对：根 `.ohmyagents/t006/output.md` 是 t008 第二轮 review 误落，与 `tasks/t006/` 第一轮产物不同。

## 追加：D10 切片 2 ARROW 清零

17 文件 72 处 U+2192 / U+2194 按语义改写（然后、对应、互转、变成）。SKIP_DIRS 外 ARROW 0；余 DASH 472。

## 追加：D10 切片 3 与归档 P0031

DASH 472：`——` 改冒号、范围符改半角、表空单元格改 `-`；S001 `——target` 误伤回改 pane target。SKIP_DIRS 外四类 0；删除 `md-char-allow.txt`；mdcharlint 零容忍无豁免尾注。归档 P0031。

## 追加：D11 归档 P0032

状态栏 projKind 加 build.zig / go.mod / CMakeLists 或 meson；图标 seti-zig E6A9、seti-go E627、seti-cpp E646（两字体 cmap）。本机临时目录实跑：zig 包版本加工具链 0.16.0、go 1.27.0、cmake/meson 包版本、Grok 无工具链、Cargo.toml 先到先得。`oma agents statusline` 已重放。归档 P0032，切 D12。D13 推远端仍待指示。

## 追加：D12 归档 P0033

`.ohmyagents/t006/output.md` 是 t008 第二轮草稿（与 `tasks/t008/output.md` 差两处措辞）；`tasks/t006/` 第一轮正本保留。已删孤儿。`task_protocol_note` 把路径写成显式 `.ohmyagents/tasks/{id}/` 并加单测。归档 P0033。GOAL / PLAN / TODO 切空。D13 仍待用户指示才推远端。

## 追加：D14 归档 P0034

用户先点名改 `.omc`，随即纠错「失误 我们的目录应该是oma」（ohmycloud 已占用 `.omc`）。项目与家目录两根改为 `.oma`；仅旧 `.ohmyagents` 在则 rename。本仓已迁；`~/.oma` 在；`oma agents statusline` 把四家 command 改到新路径。M004 数据目录行被本裁定覆盖。归档 P0034。D13 仍不推远端。

## 追加：用户指示推 origin

`git push origin main`：`68ba5a3..7211df0`。D07 至 D14 共 17 提交上远端。D13 推门槛已过，余 CI 资产与 mac `oma self update`。

## 追加：同步发现 CI 红

用户报「同步」。本地与 origin `ff8a94a` 已齐。dev-release Linux/mac Test 败在 `claude_argv_carries_bypass_flag_others_do_not` panic `validated above`（两把 env 锁互不覆盖，`plan_agents` 二次 find）。记 M049；探测改为一次。滚动 tag `dev` 仍停在 09-02，D13 mac 更新要等这次修过 CI。

## 追加：secrets 懒注入恢复加 zsh

pwsh profile IEX 空串：PATH 旧 oma 仍读已迁走的 `~/.ohmyagents`。cargo install 更新 PATH；空 vault 出 noop；pwsh 空串跳过 IEX。用户点名还少 zsh：Windows inject 只写了 `%USERPROFILE%\.zshrc`，WSL 读 `/home/ray/.zshrc` 且 PATH 无 oma。inject zsh 现写 WSL 家目录，块内回退 `oma.exe`。记 M050。明文 token 不进文档。

## 追加：ohmycloud#6 分发对齐

用户先口称 omg，随即纠正为 oma。已向 ohmycloud 发 ISSUE：如何对齐 oma 自身与四家 agent 的分发、更新、安装。<https://github.com/raystyle/ohmycloud/issues/6> 标签 coordination 加 distribution。本仓不立项（D09 已裁不管种子与下装），等 ohmycloud 回执。
