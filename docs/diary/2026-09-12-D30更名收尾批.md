# 2026-09-12：D30 更名收尾批 v0.6.1

> 当日下半场：净会话接续（repo 已更名 hst-rs、目录 D:\hst-rs），核验 D29 残面、清完发 v0.6.1。方案与过程见 `docs\proven\P0047`；本篇记流水与自省。

## 做了什么

- 交接面核验：heal hooks 检查面（settings 托管 hook 在但 shim 缺失）**已落**属实（doctor 三面 shim 在位探针加 init 必经 deploy_shims，F8 回归钉在），交接信息滞后于上一会话后半段；顺着核验盘出真残面 [实证： 当日读码]。
- 四个 sweep 连坐回归修 [实证： 当日读码加单测]：
  1. `data_dir` 被 bin 先建根挡迁移：README 装法先落 `~/.hst/bin` 建根则 rename 永不触发，老根 `~/.oma`（statusline.toml 定制与 state 历史）搁浅；修为新根仅含 bin 或空视为未初始化，旧根子项逐个迁入（撞名保新侧）。
  2. 项目 shim 退役环死码：v0.6.0 sweep 把环内文件名换成 `hst-state.*` 新名（项目级只会有旧名 `oma-state.*`）且标记查 oma 而新 shim 写 hst，两头对不上；修为新旧名双查加标记双收。
  3. SKILL marker 整串匹配断旧项目再生：v0.5.x 生成物带旧尾巴 marker（rerun oma init）不被 v0.6.0 认作生成物永不覆写；修为 hst marker 加历史前缀双收。
  4. 技能身份拧巴：v0.6.0 frontmatter 写 name: hst 但落盘目录是 `~/.claude/skills/ohmyagents/`；按 D14 裁定（技能名 ohmyagents\* 不动）统一回 ohmyagents 旧牌，main.rs 落盘路径与 skillgen 注释对齐。
- 串面清扫：README Windows 安装块断链级路径错（`.oma\bin\oma-x86_64` 加四处旧仓 URL 换 hst-rs）、doctor hooks.form 文案与五处 oma init CTA、verify 中文提示、deploy warn 与 COMMAND_MAP about 与 SKILL 正文、main 隐藏别名 help、statusline 生成脚本内注释、update 与 install 两处 UA、源注释现状描述批（历史引用与 is_ours 的 oma-state 历史识别故意保留）。R002 与 AGENTS 路由同步（hst 自管根、HST_MIRROR/HST_GATEWAY、ark hint、statusline 一级化命令）；顺手清了 ome 改 ark 的注释尾巴（warn 实串上一批已是 ark，TODO 队列该项随本版关账）。
- 门禁与 dogfood：156 单测（新增 d30 迁移与 marker 升级两用例）加 24 集成全绿；fmt 加 clippy（全存量告警）；rumdl 加 .tools 三扫描绿；本仓 init 重部署四处 SKILL `(regen)` 真实验证 marker 双收，`--pretrust` 重种新路径 trust 键（目录更名后旧键失效）doctor.blocked=false；用户级技能刷新（name: ohmyagents 加 hst 描述）。
- 发版 v0.6.1 与本机老用户迁移实证当日全过 [实证： 本机活体]：CI 两轮绿、十二资产 digest 三方对账全等（win hst 8b3063c342ee7c74）、镜像双段到货、herdr 知会 ohmycloud；迁移剧本八步（oma 0.5.4 镜像 dev 段拿 stub 替换、stub 指路 exit 1、按 README 装 hst 到 pristine 根、hst init 整根收干迁移、statusline warn 的 CTA 闭环、stub 转调 hst 0.6.1、散件 13 state 加 5 纪元前文件合并回、doctor 全绿零 warn）。细节与两项观察面（用户根旧名 shim 残留、旧 oma stable 通道匿名 API 403）见 P0047。

## 踩坑

- 文档里藏着 0x08 退格控制字符（`hst` 加 `\b` 加 `in`，某次写作把 `\b` 当转义吃掉）：肉眼读是「.hstin」，Edit 工具按文本匹配不上；用 PowerShell 控制字符级替换清掉（CHANGELOG 与 PRD 各一处）。此类坑的侦测面：mdcharlint 四类禁字不含控制字符，`rg 'hst.in'` 的 `.` 能兜住 [实证： 当日]。

## 自省

- 更名 sweep 的最大残面不在「显眼串」而在「成对语义」：文件名环与内容标记、marker 写入与检测、目录名与 frontmatter，单侧换名即断；成对面要一起过。
- 「按文档装一遍」是最好的迁移用例生成器：bin 先建根的缝就是顺着 README 安装步骤走出来的，三用例单测没覆盖这个组合。
- 技能身份（名与目录）与工具名是两个层次：D14 裁定的「ohmyagents\* 不动」在 D29 sweep 里差点被机械推翻，回滚成本一行，识别成本一次读码。

## 明日候选

- ~~本机老用户硬路径迁移实证收尾后归档 P0047~~（当日已闭环）。
- v0.7 兼容窗批：迁移转发 shim 产品化、用户根旧名清扫、别名与 stub 与旧 env 清理（ark codex pane 的 exit 1 观察入 P0047 观察面三，本机已手动缓解）。
